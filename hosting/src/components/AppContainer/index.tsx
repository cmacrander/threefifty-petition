import styled from 'styled-components';
import theme from 'components/theme';

const AppContainer = styled.div`
  width: 100%;
  max-width: ${theme.units.appWidth};

  margin: 0 auto;
`;

export default AppContainer;
